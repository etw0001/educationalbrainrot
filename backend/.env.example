# Copy to .env in this folder (or use a .env at the repo root) and fill in your keys.
# Never commit .env — it is gitignored.

OPENAI_API_KEY=
FAL_KEY=

# Optional overrides (defaults shown)
# FAL_VIDEO_MODEL=fal-ai/kling-video/v3/standard/text-to-video
# VIDEO_SEGMENT_TARGET_SECONDS=10
# VIDEO_WORDS_PER_SECOND=2.2
